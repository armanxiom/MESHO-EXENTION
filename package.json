{
  "name": "meesho-auto-lister",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "echo 'No build needed for extension'",
    "preview": "vite preview",
    "lint": "echo 'No linting required for static HTML/JS'"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
